1. Card status (and heat) do not see Grok work. The specifier pane is a 24×80 alternate-screen TUI with tmux `history_size=0`. `capture-pane -S -2000` still returns those 24 painted lines. Status then takes the last matching keyword in the last 20 of that sample. Grok stacks `You have new handoff mail. If idle, run ready_for_next.sh.` at the bottom; `\brun\b` matches that line, so the active card stays on `If idle, run ready_for_next.sh.` while tool output and thinking stay inside collapsed Grok runs and never become tmux lines. Queued cards correctly say waiting in queue. Do not treat the handoff-mail banner as status. Status and heat need a sample of the actual work, not only this 24-line chrome frame.

2. A card whose git_handoff is in Attention (`pending_approval`) still shows "waiting in queue". HTW had specifier→coder queued and held; the card correctly stayed in the specifier lane, but status only knows in-process pane text vs waiting in queue. In-process was Command Syntax, so HTW got the queue line. Holy Hand Grenade in `inbox/new` is the one actually queued. Cards waiting for operator approve/reject should say "Waiting for approval".

3. Specifier must finish the **assigned task**, then one git_handoff. HTW's New Task body was `~/junk/htw-stories` (six numbered stories). Specifier specified only `001-walk-the-numbered-dodecahedron-cave`, committed two files, queued, and `done_with_current` closed the HTW note. The other five stories never landed. The specifier prompt says for each feature: write Gherkin, QA, commit, and queue `git_handoff` to coder. That reads as one-feature-one-handoff. Combined with "small, reviewable increments," it sliced the card. The assigned task is the New Task / `TASK_NAME` + payload (the whole card), not the first file in a folder. Specify everything the card requires, then hand off once.

4. New Task must not accept a duplicate card name. It should alert and leave the dialog open so the operator can change the name. Today `pack_board create` refuses the duplicate (`Duplicate task name: HTW`) and `pack_web` treats that as `exit!`, which kills the dashboard (`Swarm disconnected`). The card is already on the board; the operator needs a warning, not a dead cockpit.

5. When a task is rejected, its card status should say **REJECTED** in red. Show a small delete button on that card; pressing it removes the card from the swimlane. Reject already drops the pending handoff, injects `Rejected: <task>`, and leaves the card in the specifier lane. Status still treats it as waiting in queue or pane text. There is no way to take a rejected card off the board.




